Slackware Current Repository by Conraid

======================================================================

python-sphinx-reredirects (Handles redirects for moved Sphinx pages)

The extension for Sphinx documentation projects that handles
redirects for moved pages. Based on the its configuration, the
extension generates HTML pages with meta refresh redirects to the
new page location.

HOME: https://github.com/documatt/sphinx-reredirects

======================================================================
