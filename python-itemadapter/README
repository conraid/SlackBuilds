Slackware Current Repository by Conraid

======================================================================

python-itemadapter (Common interface for data container classes)

The ItemAdapter class is a wrapper for data container objects,
providing a common interface to handle objects of different types in
an uniform manner, regardless of their underlying implementation.

HOME: https://github.com/scrapy/itemadapter

======================================================================
