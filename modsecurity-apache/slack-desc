# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                  |-----handy-ruler------------------------------------------------------|
modsecurity-apache: ModSecurity (ModSecurity v3 Apache Connector)
modsecurity-apache:
modsecurity-apache: The ModSecurity-apache connector is the connection point between
modsecurity-apache: Apache and libmodsecurity (ModSecurity v3). Said another way, this
modsecurity-apache: project provides a communication channel between Apache and
modsecurity-apache: libmodsecurity. This connector is required to use LibModSecurity
modsecurity-apache: with Apache.
modsecurity-apache:
modsecurity-apache:
modsecurity-apache:
modsecurity-apache:
