Slackware Current Repository by Conraid

======================================================================

python-ftputil (High-level FTP client library for Python)

The ftputil Python library is a high-level interface on top of the
ftplib module. The FTPHost objects generated with ftputil allow many
operations similar to those of os, os.path and shutil.

HOME: https://ftputil.sschwarzer.net/

======================================================================
