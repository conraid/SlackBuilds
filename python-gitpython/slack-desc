# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                |-----handy-ruler------------------------------------------------------|
python-gitpython: gitpython (Python git library)
python-gitpython:
python-gitpython: GitPython is a python library used to interact with Git repositories.
python-gitpython: GitPython provides object model read and write access to your git
python-gitpython: repository. Access repository information conveniently, alter the
python-gitpython: index directly, handle remotes, or go down to low-level object
python-gitpython: database access with big-files support.
python-gitpython:
python-gitpython:
python-gitpython:
python-gitpython:
