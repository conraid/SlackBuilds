# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

         |-----handy-ruler------------------------------------------------------|
wine-mono: wine-mono (Wine's built-in replacement for MS .NET Framework)
wine-mono:
wine-mono: Wine Mono is a package containing Framework Mono and other projects,
wine-mono: intended as a replacement for the .NET Framework (4.8.1 and earlier)
wine-mono: in Wine. It works in conjunction with Wine's builtin mscoree.dll,
wine-mono: and it is not intended to be useful for any other purpose.
wine-mono:
wine-mono:
wine-mono:
wine-mono:
wine-mono:
