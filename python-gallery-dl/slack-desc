# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                 |-----handy-ruler------------------------------------------------------|
python-gallery-dl: python-gallery-dl (command-line program to download image galleries)
python-gallery-dl:
python-gallery-dl: gallery-dl is a command-line program to download image galleries and
python-gallery-dl: collections from several image hosting sites (see Supported Sites).
python-gallery-dl: It is a cross-platform tool with many command-line and configuration
python-gallery-dl: options, as well as powerful file-naming capabilities.
python-gallery-dl:
python-gallery-dl:
python-gallery-dl:
python-gallery-dl:
python-gallery-dl:
