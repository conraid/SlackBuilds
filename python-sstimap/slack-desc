# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

              |-----handy-ruler------------------------------------------------------|
python-sstimap: python-sstimap (Automatic SSTI detection and interactive tool)
python-sstimap:
python-sstimap: SSTImap is a penetration testing software that can check websites
python-sstimap: for Code Injection and Server-Side Template Injection vulnerabilities
python-sstimap: and exploit them, giving access to the operating system itself.
python-sstimap:
python-sstimap:
python-sstimap:
python-sstimap:
python-sstimap:
python-sstimap:
