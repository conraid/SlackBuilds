# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

        |-----handy-ruler------------------------------------------------------|
rt-tests: rt-tests (suite of real-time tests)
rt-tests:
rt-tests: A set of utilities to test Linux real-time properties.
rt-tests: Includes:
rt-tests: - cyclictest: measures high resolution timer latency
rt-tests: - hackbench: scheduler stress test
rt-tests: - hwlatdetect: finds hardware-induced latencies
rt-tests: - pip_stress: priority inheritance stress test
rt-tests:
rt-tests:
rt-tests:
