
The Slackers.it Repository 

======================================================================

conntrack-tools (userspace tools for conntrack)

The conntrack-tools are a set of userspace tools that allow
system administrators to interact with the Connection
Tracking System (the module that provides stateful packet 
inspection for iptables).

The conntrack-tools include the userspace daemon conntrackd 
and the command line interface conntrack.

Home: http://conntrack-tools.netfilter.org
======================================================================
Requires libnetfilter_conntrack, libnfnetlink, libmnl,
libnetfilter_cttimeout and a kernel >= 2.6.18
To support the nfnetlink_cttimeout subsystem kernel requires >= 3.4.0
