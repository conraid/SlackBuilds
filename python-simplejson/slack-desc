# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                 |-----handy-ruler------------------------------------------------------|
python-simplejson: simplejson (extensible JSON encoder and decoder for Python)
python-simplejson:
python-simplejson: Simplejson is a simple, fast, complete, correct and extensible JSON
python-simplejson: encoder and decoder for Python.
python-simplejson: It is pure Python code with no dependencies, but includes an optional
python-simplejson: C extension for a serious speed boost.
python-simplejson:
python-simplejson:
python-simplejson:
python-simplejson:
python-simplejson:
