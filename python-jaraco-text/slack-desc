# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                  |-----handy-ruler------------------------------------------------------|
python-jaraco-text: python-jaraco-text (Module for text manipulation)
python-jaraco-text:
python-jaraco-text: This package provides handy routines for dealing with text, such as
python-jaraco-text: wrapping, substitution, trimming, stripping, prefix and suffix
python-jaraco-text: removal, line continuation, indentation, comment processing,
python-jaraco-text: identifier processing, values parsing, case insensitive comparison,
python-jaraco-text: and more. See the docs (linked in the badge above) for the detailed
python-jaraco-text: documentation and examples.
python-jaraco-text:
python-jaraco-text:
python-jaraco-text:
