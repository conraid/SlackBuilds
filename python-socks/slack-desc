# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

            |-----handy-ruler------------------------------------------------------|
python-socks: python-socks (Core proxy client functionality for Python)
python-socks:
python-socks: The python-socks package provides a core proxy client functionality
python-socks: for Python. Supports SOCKS4(a), SOCKS5(h), HTTP CONNECT proxy and
python-socks: SOCKS4(a), SOCKS5(h), HTTP CONNECT proxy and provides sync and async
python-socks: (asyncio, trio, curio, anyio) APIs.
python-socks:
python-socks:
python-socks:
python-socks:
python-socks:
