# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

             |-----handy-ruler------------------------------------------------------|
python-svglib: python-svglib (A pure-Python library for reading and converting SVG)
python-svglib:
python-svglib: Svglib is a Python library for reading SVG files and converting them
python-svglib: (to a reasonable degree) to other formats using the ReportLab Open
python-svglib: Source toolkit.
python-svglib:
python-svglib:
python-svglib:
python-svglib:
python-svglib:
python-svglib:
