
The Slackers.it Repository 

======================================================================

unibilium (a terminfo parsing library )

Unibilium is a very basic terminfo library. It doesn't depend on 
curses or any other library. It also doesn't use global variables,
so it should be thread-safe.

HOME: https://github.com/mauke/unibilium/

======================================================================
