# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

               |-----handy-ruler------------------------------------------------------|
python-aws-cli2: python-aws-cli2 (Amazon Web Services Command Line Interface v2)
python-aws-cli2:
python-aws-cli2: This package provides the AWS CLI v2 installed in an isolated
python-aws-cli2: environment in /opt/aws-cli2. This approach avoids conflicts
python-aws-cli2: with system-wide Python libraries and ensures all specific
python-aws-cli2: dependencies (like awscrt and botocore) are bundled at the
python-aws-cli2: exact versions required by Amazon.
python-aws-cli2:
python-aws-cli2: Access the tool globally using the 'aws' command.
python-aws-cli2:
python-aws-cli2:
